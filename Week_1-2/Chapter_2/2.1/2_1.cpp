double x, y, z;
if ((x > y) || (x < 5.0))
{
    // 1. Compares x to y and 5. If it larger than y or less than 5.0 it sets z to 4.0. Else it sets it to 2.0
    // 2.a z = 4.0
    // 2.b z = 2.0
    // 2.c z = 4.0
    // 3. You would just say (x >= y) instead of (x > y)
    z = 4.0;
}
else
{
    z = 2.0;
}






